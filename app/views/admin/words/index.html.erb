<% provide(:title, 'All words') %>
<h1>All words</h1>
<%= will_paginate %>
<ul class="users">
  <% @words.each do |words| %>
 	<li>
  <%= words.name %>
     <%= link_to "Delete", admin_words_path(category), method: :delete,
                                  data: { confirm: "Are you sure?" }, class: "btn btn-success" %>
     <%= link_to "Edit",edit_admin_words_path(category), class: "btn btn-success"%>
</li>
  <% end %>
</ul>

<%= will_paginate %>